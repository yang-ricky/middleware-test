{
  "name": "middleware-test",
  "version": "1.0.0",
  "description": "This repo focus on verifying if middleware works fine * redis * kafka * mysql * postgres * elasticsearch",
  "main": "redis.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://hub.fastgit.org/yang-ricky/middleware-test.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql": "^2.18.1",
    "redis": "^3.1.2"
  }
}
